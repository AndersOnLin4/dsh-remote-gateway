name: dsh_gateway
description: "DSH 远程网关 Android 客户端"
publish_to: 'none'
version: 0.1.5+6

environment:
  sdk: ^3.13.0

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  flutter_secure_storage: ^9.2.0
  shared_preferences: ^2.3.0
  flutter_local_notifications: ^17.2.4
  url_launcher: ^6.3.0
  webview_flutter: ^4.13.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
