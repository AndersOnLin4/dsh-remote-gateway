# DSH 远程网关 — App 化实施方案（评审稿）

> 目标：把 `dsh-remote-gateway` 从"手机/电脑浏览器里打开网页"升级为
> **Android 原生 App（出门远控）+ Windows 桌面 App（本机掌控）** 双形态，
> 彻底告别浏览器操作的不顺手。
> 本文为方案评审稿，**本阶段只出方案，不写代码**。评审通过后按里程碑执行。

---

## 〇、现状速览（已实地核实仓库代码）

| 层 | 现状 |
|---|---|
| 网关后端 | Python 3.10+ / FastAPI / uvicorn，监听 `0.0.0.0:8080`，登录走 HMAC 签名 Cookie |
| 网关前端 | 纯 HTML/JS/CSS 移动端单页（无构建链），三 Tab：监控 / 会话 / 日志 + 会话预览浮层 |
| 通信方式 | **全部是前端轮询**（状态 5s、会话列表 15s、待答问题 3s、回复后追 tail），**没有任何服务端推送通道** |
| 对外接口 | `/gw/login` `/gw/logout` `/gw/session` `/gw/status` `/gw/sessions` `/gw/session/{sid}/tail|send|answer` `/gw/questions` `/gw/log` `/gw/control/{start\|stop\|restart}`，其余路径反向代理 DSH 原版 UI（HTTP+WebSocket） |
| 配置 | `gateway/.env`（`GATEWAY_PASSWORD`、`GATEWAY_SECRET_KEY`、`HARNESS_ROOT`、`DSH_HOME`），本机可读 |
| 启动方式 | `install.cmd` → `start-everything.cmd` → `stop-everything.cmd`（多个 cmd/ps1 + 终端窗口 + Tailscale 唤醒） |
| 进程托管 | 网关以子进程拉起 `node dsh web --host 127.0.0.1:3080`，日志落 `G:\harness\logs\dsh.log` |

**核心结论：做成 App 完全可行。** 网关已经把"DSH 侧的所有脏活"（会话解析、zstd 读取、数据过滤、Host/Origin 重写、问题捕获）封装成了干净的 REST 接口 —— **两个 App 都只需要对接同一个 HTTP API，不复制任何 DSH 内部逻辑**，网关成为唯一的后端。工作量主要花在"新界面 + 推送通道 + 进程守护"三件事上。

---

## 一、总体架构（App 化之后长这样）

```
                ┌────────────────────────────────────────────┐
                │   Windows 电脑                              │
 Android App ──►│                                            │
 (出门远控)      │   ┌──────────────────────────────┐         │
                │   │  Windows 桌面 App (托盘+守护)  │         │
                │   │  管理/拉起/守护 网关 与 DSH     │         │
                │   └──────────────┬───────────────┘         │
                │                  ▼ 仅回环                   │
                │   ┌──────────────────────────────┐         │
                │   │  FastAPI 网关 0.0.0.0:8080    │◄── 唯一对外入口
                │   │  (现有代码 + 轻量增量)         │         │
                │   └──────────────┬───────────────┘         │
                │                  ▼ 仅回环(127.0.0.1)        │
                │   ┌──────────────────────────────┐         │
                │   │  DSH dsh web :3080（永不外露） │         │
                │   └──────────────────────────────┘         │
                └────────────────────────────────────────────┘
                          ▲ 公网可达只有"网关"（现有安全模型不变）
```

- **Android App**：直连网关 REST API（局域网 IP 或 Tailscale 域名），**新增** `/gw/events` 推送通道 + 本地通知。
- **Windows 桌面 App**：本机掌控，直连 `127.0.0.1:8080`（可读 `.env` 免输密码自动登录），**接管现有 cmd 脚本**，成为网关与 DSH 的"守护进程"。
- 网关安全模型（只暴露 8080、DSH 永绑 127.0.0.1、登录限速、审计）**一行不动**。

---

## 二、A 线：Android 手机 App（重点）

### A1. 产品目标与增量价值

| 痛点（网页版） | App 解决方式 |
|---|---|
| 没推送，agent 提问了没人知道 | 网关事件流 + 本地通知，秒弹"agent 正在问你" |
| 必须打开浏览器/手动刷新 | 常驻后台，推送直达，点通知直达会话 |
| 手机上网页交互别扭（加载、滚动、误触） | 原生组件：列表、气泡、选择卡片、手势 |
| 多台电脑要记 IP/密码 | 内置"服务器配置"管理，密码存 Android Keystore 加密区 |
| 弱网/切后台断线 | WebSocket 自动重连 + 断线重推兜底 |

### A2. 技术选型（三档，按推荐排序）

| 方案 | 语言 | 体积/发行 | 体验 | 工作量 | 说明 |
|---|---|---|---|---|---|
| **A1-1 原生 UI：Flutter（推荐）** | Dart | APK ~20MB，直接侧载/GitHub Releases | 最佳：原生列表/手势/通知 | 中 | 一套代码未来可顺手出 iOS；中文生态和通知/WebSocket 插件成熟 |
| A1-2 原生 UI：React Native | JS/TS | 同上 | 接近原生 | 中 | 你要是更熟 JS 就选它，其余和 Flutter 等价 |
| A2 混合壳：Capacitor/WebView 套现有网页 | JS（复用现有 index.html） | APK ~5MB | 界面仍是网页，仅解决"装 App + 推送" | **低** | 最快验证版；交互体验不升级，不解决"网页难受" |
| A3 PWA（不写原生） | 现有前端 | 无安装包 | 不变 | 极低 | 仅"添加到主屏幕"，体验提升有限，**不建议作为终点** |

**推荐：A1-1 Flutter。** 理由：单独开发者、一次投入换双端可能、推送/后台/WebView 生态最成熟。下文按 Flutter 规划，若你选 RN 架构完全照搬（只有 UI 层语言不同）。

### A3. 功能清单（对齐全量现有接口）

| 模块 | 页面/能力 | 对接接口（现有） |
|---|---|---|
| 登录 | 登录页 + 服务器管理（名称/地址/记住密码） | `POST /gw/login`（Cookie 存安全存储） |
| 监控页 | 状态灯、工作中会话、统计数字、启/停/重启（二次确认） | `GET /gw/status`、`POST /gw/control/*` |
| 会话页 | 会话列表（标题/工作区/轮数/token/时间、工作态高亮） | `GET /gw/sessions` |
| 会话详情 | 气泡式预览（过滤后内容）、**直接回复**、等待轮询刷新 | `GET /gw/session/{sid}/tail`、`POST /gw/session/{sid}/send` |
| 选择题 | 收到提问弹卡片：选项高亮、单选/多选、一键提交 | `GET /gw/questions?sid=`、`POST /gw/session/{sid}/answer` |
| 日志页 | DSH 实时日志、自动滚动 | `GET /gw/log?n=` |
| 完整控制台 | 内嵌 WebView 直达 DSH 原版 UI（先注入 `gw_session` Cookie 再导航） | 反向代理路径（现有，无需改动） |
| 推送（新增能力） | 连接 `/gw/events`，事件驱动本地通知；切后台保活 | **`/gw/events`（新增，见第四节）** |

### A4. 关键设计决策

1. **鉴权**：沿用现有"密码换 Cookie"流程，Cookie 值存在 **flutter_secure_storage（Android Keystore 加密）**，每次请求由持久化 Cookie 客户端自动携带。换服务器=换地址+重登录，多服务器并存。
2. **完整控制台怎么进**：App 内 WebView 先调 `POST /gw/login` 拿到 Cookie，注入到 WebView 的 CookieManager 后导航到 `/dashboard` 或代理路径。网关的 Cookie 是 `SameSite=Lax`，同 Host 内没问题（连的就是网关本身）。
3. **推送通道（本次方案的最大增量）**：三档实现，按需叠加——
   - **第一档（必做，自包含）**：网关新增 `GET /gw/events`（SSE 或 WebSocket，Cookie 鉴权），事件：`status-changed` / `new-question` / `reply-updated` / `dsh-down` / `log-line`。App 用前台服务保持连接（同一网络下蜂窝也能连，因为 Tailscale 在任何网络都通），事件到达即弹本地通知。零第三方依赖。
   - **第二档（可选，弱网兜底）**：网关把关键事件转发到 **ntfy**（免费、免注册，`GATEWAY_NTFY_TOPIC/TOKEN` 配在 `.env`），App 在监听不到网关时改听 ntfy 通道，保证"人不常在"也能被叫醒。
   - **第三档（远期，上架用）**：FCM，需 Firebase 工程 + 网关调 FCM HTTP API，等真要上 Play Store 再考虑。
4. **省电策略**：前台服务仅在"已登录且网络可用"时保活，`/gw/events` 空闲 30s 心跳；Android 电池优化白名单引导用户加入。
5. **发行**：GitHub Releases 发 APK 侧载即可（与现有 Release 流程一致），不需要 Play Store。

### A5. 里程碑（单人兼职估算）

| 里程碑 | 内容 | 验收 | 工作量 |
|---|---|---|---|
| A-M1 | 环境准备（Flutter SDK + Android SDK 安装、APK keystore 签名）+ 工程搭建 + 登录 + 服务器管理 + 安全存储 | 真机侧载，连自家网关登录成功 | ~1 周 |
| A-M2 | 监控 / 会话 / 日志三页（复用现有接口，含控制按钮二次确认） | 手机上看到真实状态并可启停 | ~1 周 |
| A-M3 | 会话预览 + 回复 + 选择题作答 | 手机完整走通"提问→作答→回复"闭环 | ~1 周 |
| A-M4 | 网关 `/gw/events` + 前台服务 + 本地通知 + 自动重连 | 手机锁屏时 agent 提问能弹出通知 | ~1 周 |
| A-M5 | 完整控制台 WebView + 打磨（深色模式、触控优化、离线提示、多服务器） | 全流程验收 | ~1 周 |
| **合计** | | | **约 4~5 周** |

---

## 三、B 线：Windows 桌面 App（本机掌控）

### B1. 产品目标与增量价值

| 痛点（现状） | 桌面 App 解决方式 |
|---|---|
| 启动要双击多个 cmd，黑窗乱飞 | 单 exe：托盘图标 + 一键"启动/停止/重启/打开" |
| 网关或 DSH 崩了没人看 | **守护进程**：检测崩溃自动拉起，托盘变红提示 |
| 开机要用计划任务手动建 | 内置"开机自启"开关（注册表 Run/计划任务），一键关闭 |
| 想看一眼状态还得开浏览器输密码 | 桌面窗口自动登录（本机直接读 `.env` 密码） |

### B2. 技术选型（按推荐排序）

| 方案 | 体积 | 语言 | 说明 |
|---|---|---|---|
| **B-1 Electron（本次选定 ✅）** | ~150MB+ | 纯 JS/TS（Node 24 已装） | 无额外工具链门槛、与网关前端/DSH 同技术栈，托盘·自启·进程守护全部内置能力，最快跑起来 |
| B-2 Tauri 2 | ~15MB | Rust 壳 + 前端复用现有静态页 | 体积/内存最优，但需先装 Rust + MSVC 工具链（数 GB 下载）；留作后续"瘦身"迁移选项 |
| B-3 Python 托盘（pystray/tkinter） | ~50MB(PyInstaller) | Python | 与网关同语言可复用逻辑，但打包体验差、界面简陋 |
| B-4 .NET WPF/WinUI | ~10MB | C# | 原生集成最佳，但要新学语言 |

**选定：B-1 Electron。** 环境实测本机无 Rust 工具链，而 Node 24 已装、全栈 JS 技术栈——个人工具以交付速度为重；未来想瘦身再迁 Tauri（架构不变，只换壳）。

### B3. 功能清单

| 能力 | 说明 |
|---|---|
| 托盘图标 | 常驻托盘：绿=正常 橙=DSH 异常 红=网关挂；右键菜单：启动/停止/重启 DSH、打开控制台、打开仪表盘、开机自启、退出 |
| 进程守护 | 守护两个进程：网关(uvicorn) 与 DSH(`node dsh web`)；崩溃自动重启并审计；**替代 `start-everything.cmd`** |
| 网关托管 | 桌面 App 直接查端口：3080 未监听→拉起 DSH；8080 未监听→拉起 uvicorn 网关（复用 `gateway\app` 源码或打包的 venv） |
| 桌面窗口 | 内嵌 WebView2 打开 `http://127.0.0.1:8080/dashboard`；本机直读 `gateway\.env` 的密码，**先自动登录再进仪表盘**，无需输密码 |
| 自启/关闭 | 一键开启/关闭开机自启；退出时可选择"仅退桌面端"或"连 DSH 一起停"（对齐现有 `stop-everything.cmd`） |
| 日志 | 托盘菜单直达 DSH 日志目录；窗口内也有日志页（复用 `/gw/log`） |

> 桌面 App 与 Android App 共用同一套网关 REST API —— 桌面端不新增任何 DSH 逻辑，只做"进程守护 + 门口的壳"。

### B4. 里程碑（单人兼职估算）

| 里程碑 | 内容 | 验收 | 工作量 |
|---|---|---|---|
| B-M1 | 工程搭建 + 托盘图标 + 状态检测 + 启动/停止/重启 | 托盘能管 DSH 启停、状态正确 | ~1 周 |
| B-M2 | 进程守护 + 网关托管 + 开机自启 | 杀掉 DSH 进程能被自动拉起；重启电脑后自动恢复 | ~1 周 |
| B-M3 | 桌面窗口（自动登录仪表盘）+ 退出策略 + 打磨 | 一个 exe 完成 "启动→监控→打开控制台→优雅退出" | ~1 周 |
| **合计** | | | **约 2~3 周** |

---

## 四、A 线前置的网关侧增量（小而向后兼容；B 线不需要，随 A 线落地）

现有接口一个不改，只做**增加**：

| 增量 | 说明 | 用途 |
|---|---|---|
| `GET /gw/events`（SSE/WebSocket，Cookie 鉴权） | 推送：`status-changed` / `new-question` / `reply-updated` / `dsh-down` / 心跳 | Android 推送通道；未来网页版也可改推送代替轮询 |
| `.env` 新增 `GATEWAY_NTFY_TOPIC` / `GATEWAY_NTFY_TOKEN`（可选） | 关键事件转发 ntfy | Android 弱网兜底推送 |
| 事件捕获点 | 复用现有 `question_watcher` + 进程探活，加 `asyncio.Queue` 广播即可 | 工作量约 2~3 天 |
| README 增补"接口契约"小节 | 把现有 REST 稳定清单写成文档 | 两个 App 开发都能照着写 |

> 风险最小化：增量全部向后兼容，网页版继续可用，改坏了回滚成本为零。

---

## 五、风险与边界

| 风险 | 对策 |
|---|---|
| iOS 推送/上架成本高 | 本方案只做 Android；Flutter 代码留了 iOS 后路 |
| 手机后台保活被系统杀 | 前台服务 + 电池白名单引导；事件流 30s 心跳；ntfy 兜底 |
| WebView 内层原版 DSH UI 手机体验一般 | 保持"常规操作走原生界面，WebView 仅兜底完整控制台"的定位 |
| 桌面 App 守护逻辑写坏网关 | 守护仅"按端口探测 + 拉起"，与网关解耦；先手动模式验证再开自动 |
| Tailscale 未登录时推不到 | 推送事件流本机可达即可用；外网依赖 Tailscale/ntfy 的既有说明 |
| 多端同时操作冲突 | 沿袭现状：无锁，操作以最后提交为准；审计日志已有留痕 |
| 开发期误伤正在运行的 DSH（本机 3080 正服务着工作会话/本会话 GUI） | **开发守则：绝不对已监听的 3080 做 stop 测试**；守护逻辑"只补位不抢占"（端口已监听则不重复拉起、不强杀）；需测启停时用独立测试端口（如 3090）拉起一次性 DSH |

---

## 六、实施顺序（评审定稿：先 B 后 A）

1. **第 1 步（B 线，2~3 周）**：Windows 桌面 App —— 最快兑现"告别 cmd + 浏览器"，桌面端同时成为网关/DSH 的守护者，给 A 线一个稳定底座。**B 线不依赖任何网关改动，开工即所得。**
2. **第 2 步（A 线前置，2~3 天）**：网关增量 `/gw/events` + 事件广播 —— 仅随 A 线落地（B 线不需要）。
3. **第 3 步（A 线，4~5 周）**：Android App —— 推送通知 + 原生交互落地。

---

## 七、代码落位与开发环境（评审补充）

- **代码落位**：仓库内新增 `desktop/`（B 线）与 `app/`（A 线），与 `gateway/` 平级，随仓库 git 发布；桌面 App 安装包沿用 GitHub Releases 流程。
- **B 线环境**：Node 24 + npm 已具备，无新增工具链；现有网关安装目录 `G:\Andersonlin4-design 手机远控项目\gateway`（端口 8080 已在运行）由桌面端"收养"识别，只补位不抢占。
- **A 线环境（前置条件）**：Flutter SDK + Android SDK（合计约 2~4 GB 下载）+ APK keystore 签名，在 A-M1 内完成，不阻塞 B 线。
- **桌面 App 兼容**：首次运行若找不到 `gateway/.env`（网关未安装），提示先跑 `install.cmd` 或手动配置路径，不强制。

---

## 八、结论

- **机会：完全有。** 现有网关已是一套干净的 REST 后端，App 化不需要碰 DSH 内部逻辑。
- **两条产品线：** Android App（远控推送，工作量约 4~5 周）+ Windows 桌面 App（托盘守护，工作量约 2~3 周）。
- **A 线地基：** 网关加一个 `/gw/events` 推送通道（2~3 天）；**B 线零网关改动**，全部复用现有接口与安全模型。
- **无需改动：** 认证、限速、审计、反向代理、DSH 仅回环监听 —— 安全边界原封不动。

---

*评审确认项：① 技术选型确认（Flutter？RN？桌面 Tauri 还是 Electron？）；② 优先做桌面端还是 Android 端；③ 推送是否接受"ntfy 免费中继"这一轻第三方依赖。*