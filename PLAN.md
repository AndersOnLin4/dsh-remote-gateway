# 手机远控 DeepSeek Harness — 项目实现方案

> 目标：出门在外也能用手机查看电脑上 DSH 的运行情况，并在需要时远程操作它。
> 局域网内直接可访问，出门在外走流量也能访问，同时满足基本安全性。
> 本文件为方案评审稿，评审通过后按阶段执行。

---

## 一、现状调研结论（已实地确认）

| 项 | 结论 |
|---|---|
| 入口 | `start-dsh.cmd` 设置环境后执行 `dsh web`，浏览器 UI 跑在 `http://127.0.0.1:3080` |
| 监听地址 | `dsh web` 支持 `--host` / `--port`，但 host 只接受 `127.0.0.1` 或 `0.0.0.0` |
| **鉴权** | **DSH 官方明确没有任何鉴权层**（README：`No TLS, auth, or origin policy`），`dsh web --host 0.0.0.0` 被官方标注"在认证层出现之前不支持" |
| 信任栅栏 | `/api` 有 browser-trust fence（校验 Host/Origin，防 DNS rebinding），但那是"可达性策略"**不是认证** |
| 权限预设 | 当前 `settings.yaml` 权限 preset 为 `danger-full-access`（可执行任意命令） |
| 敏感数据 | `dsh-home/.credentials.yaml` 存有 API 密钥；`/api` 特权方法集（credentials/settings 读写等）在空信任表时仅限回环本机 |
| 会话数据 | `dsh-home/sessions/<workspace>/<session-id>/session.jsonl.zstd`（zstd 压缩 JSONL） |

**最核心的结论：绝不能把 3080 直接暴露到局域网或公网。所有远程访问都必须经过一个我们自己加的认证层。**

---

## 二、总体架构

```
┌─────────────┐  LAN / Tailscale / Tunnel
│  手机浏览器   │─────────────────────────────┐
└─────────────┘                              ▼
                                    ┌──────────────────────────┐
                                    │   FastAPI 网关   :8080    │
                                    │  ├─ 登录鉴权(密码→会话)    │
                                    │  ├─ /api 监控+控制接口     │
                                    │  ├─ 移动端仪表盘(静态页)    │
                                    │  └─ /console/* 反向代理     │
                                    └────────────┬─────────────┘
                                                 │ 仅回环访问
                                                 ▼
                                    ┌──────────────────────────┐
                                    │  DSH  `dsh web`  127.0.0.1:3080 │
                                    │  永不监听 0.0.0.0            │
                                    └──────────────────────────┘
```

能力分三层，按风险递增：
1. **只读监控** — 运行状态、会话列表、日志实时查看
2. **受控操作** — 启动 / 停止 / 重启 DSH（带二次确认）
3. **完整远程操作** — 通过反向代理复用 DSH 原版 Web UI（聊天、审批、看轨迹）

DSH 本身保持"仅回环监听"不变，网关是唯一对外入口，认证全部由网关负责。

---

## 三、功能清单

### 监控（只读）
- [ ] DSH 进程是否存活、3080 端口探测、Uptime
- [ ] 会话列表（工作区、会话数、各会话文件大小 / 最后活动时间）
- [ ] 日志实时查看（DSH 进程 stdout 落盘 + 自动刷新，可选 SSE 推送）
- [ ] 最近活动时间（判断是否卡死/空转）

### 控制
- [ ] 启动 / 停止 / 重启 DSH（网关以子进程托管，日志重定向到 `G:\harness\logs\`）
- [ ] 开机自启（Windows 计划任务）

### 远程操作
- [ ] "打开完整控制台"按钮 → 代理到原版 DSH UI（HTTP + WebSocket）
- [ ] 手机上可发指令、查看会话、处理审批

### 安全
- [ ] 单密码登录 → HMAC 签名会话 Cookie（非明文，超时过期）
- [ ] 登录失败限速（防爆破）
- [ ] 控制类接口全部要求登录；前端二次确认弹窗
- [ ] 操作审计日志（谁、何时、做了什么）落盘到网关日志

---

## 四、技术栈与目录结构

**后端**：Python + FastAPI + uvicorn（本机已有 G 盘 node 环境，Python 需确认版本，无则装 3.11+）
**代理**：httpx（HTTP）+ WebSocket 升级转发（重写 Host / Origin 以通过 DSH 的 browser-trust fence）
**前端**：单页 HTML + 原生 JS + CSS（移动优先、暗色主题，无构建步骤）
**数据**：不新增数据库，监控数据实时读文件系统

```
G:\Andersonlin4-design 手机远控项目\
├─ PLAN.md                       ← 本方案
└─ gateway\
   ├─ requirements.txt
   ├─ run.py                     # 启动入口
   ├─ start-all.cmd              # 一键启动（网关 + 按需拉起 DSH）
   ├─ app\
   │  ├─ main.py                 # FastAPI 应用与路由
   │  ├─ auth.py                 # 登录 / 会话签发与校验 / 限速
   │  ├─ dsh_manager.py          # DSH 进程检测与启停托管、日志落盘
   │  ├─ monitor.py              # 会话列表 / 端口探测 / 日志读取
   │  ├─ proxy.py                # 反向代理到 127.0.0.1:3080（含 WebSocket、Host/Origin 重写）
   │  └─ config.py               # 配置（端口、密码、路径）
   └─ static\
      ├─ index.html              # 移动端仪表盘（登录 / 状态 / 日志 / 控制）
      ├─ app.js
      └─ style.css
```

---

## 五、实施步骤（分阶段，每阶段可验证）

- **Phase 0 环境准备**：确认/安装 Python、pip 装依赖；测通局域网访问本机端口。
- **Phase 1 网关骨架 + 登录**：FastAPI 起 8080，绑定 `0.0.0.0`；登录页 + 会话 Cookie + 限速。验证：手机连局域网 Wi-Fi 能登录。
- **Phase 2 监控接口 + 仪表盘**：`/api/status`（进程/端口/uptime）、`/api/sessions`（列表）、移动端仪表盘展示。验证：手机上看到真实状态。
- **Phase 3 DSH 托管启停 + 日志**：`dsh_manager` 启动/停止/重启；stdout 落盘 `G:\harness\logs\dsh.log`；日志 tail 接口。验证：手机远程重启 DSH 成功。
- **Phase 4 反向代理原版 UI**：`/console/*` 代理到 3080，处理 WebSocket 与 Host/Origin 重写（过 browser-trust fence）。验证：手机进入原版 DSH UI 可发消息。
- **Phase 5 局域网验收**：全程只用 Wi-Fi 走一遍 监控→控制→操作 全流程。
- **Phase 6 外网方案落地**：按第六节选定方案接通。
- **Phase 7 加固与自启动**：开机自启、日志轮转、审计日志、密码轮换提醒。

---

## 六、外网访问方案对比

| 方案 | 成本 | 难度 | 安全 | 手机端体验 | 说明 |
|---|---|---|---|---|---|
| **Tailscale（推荐）** | 免费 | 低 | 高（公网零暴露） | 需装 App，登录后直接访问 | 加密组网，**无任何公网端口**，天然防扫描 |
| Cloudflare Tunnel | 免费（quick tunnel）；稳定域名需自购域名 | 低 | 中（公网暴露，靠登录页兜底） | 免装 App，浏览器直达 | 无需开端口；quick tunnel 每次 URL 随机 |
| frp 内网穿透 | 需一台 VPS | 中 | 中低 | 浏览器直达 | 需要服务器和运维 |
| 路由器端口映射 | 零 | 中 | 低 | 浏览器直达 | 公网暴露端口，不推荐 |

**结论**：
- **默认走 Tailscale**：免费、安全最高、难度最低；电脑装 Tailscale，手机装 App 同一账号，手机流量/任何网络直接访问 `http://<电脑TailscaleIP>:8080`。
- **备选 Cloudflare quick tunnel**：如果不想在手机装任何 App，用 `cloudflared` 一键起一条免费公网隧道指向本地 8080，登录页负责鉴权（此时必须配合强密码 + 限速，因为隧道地址是公网可达的）。

> 两者在网关层不需要任何代码差异，只影响"8080 怎么被外网找到"。

---

## 七、安全设计（重点）

1. **DSH 永不监听 `0.0.0.0`**，始终只绑 `127.0.0.1`。公网可达的永远只有网关。
2. **认证集中在网关一层**：登录密码 → HMAC 签名会话 Cookie（带过期时间），所有 `/api` 与 `/console` 请求先过鉴权。
3. **登录限速 + 审计**：连续失败锁定一段时间；成功/失败、控制操作全部记日志。
4. **危险操作二次确认**：启停/重启按钮前端二次确认，接口侧再校验一次会话有效性。
5. **防火墙最小化**：走 Tailscale 时网关端口只对 Tailscale 网卡可达（甚至可只对 tailnet 开放）；走 Tunnel 时无需开放任何入站端口。
6. **凭据风险控制**：`credentials.yaml` 不应被远程会话读取——DSH 特权方法集在空信任表下已钉在回环本机，我们代理时不声明 `trustedHosts`，保持该默认。远程操作只具备"当前会话能力"（danger-full-access 下可执行命令，这是用户主动授权的远程操控能力）。
7. **可选加固**：远程期间临时把权限 preset 降到 `safe-autonomous` 或设审批（改 `settings.yaml`），需要时再升回来。
8. **密码策略**：至少 16 位随机密码，写在网关配置里（`gateway\.env`，加入 gitignore），定期轮换。

---

## 八、部署与自启动

- `start-all.cmd`：检查 3080 是否已监听 → 未监听则拉起 `dsh web`（stdout 重定向到 `G:\harness\logs\dsh.log`）→ 再启动 uvicorn 网关（8080）。
- Windows 计划任务：登录时/开机时运行 `start-all.cmd`（最小化/隐藏窗口）。
- 日志轮转：网关日志按天切割，DSH 日志限制大小（超限归档）。
- 若同时存在 `start-dsh.cmd` 的手动启动习惯，网关检测 3080 已占用则不再重复拉起（避免双实例）。

---

## 九、风险与边界

| 风险 | 对策 |
|---|---|
| 手机流量下 WebSocket 长连接易断 | 前端自动重连、刷新按钮 |
| DSH 版本升级可能改动 `/api` 路径或 trust 逻辑 | 代理层集中处理，升级后回归测试 Phase 4 |
| 远程操控 = 远程执行代码（danger-full-access） | 明确认知；可选临时降权/审批；审计日志留痕 |
| 局域网 IP 变化导致手机书签失效 | 用 Tailscale 固定 IP 或内网静态 IP；备选 mDNS（`harness.local`） |
| 登录密码泄露 | 限速 + 审计 + 定期轮换；Tunnel 方案下避免把 URL 随意传播 |

---

## 十、后续可扩展（非本期）

- 推送告警：DSH 崩溃 / 长时间无输出时推送到手机（Server酱 / 钉钉 / Telegram）
- 用量统计：token 消耗、会话成本面板
- 多用户 / 多设备密钥管理
- 内网 mDNS 域名访问（免记 IP）

---

*评审确认项：① 默认外网方案选 Tailscale 还是 Cloudflare Tunnel；② 远程操控期间是否接受 danger-full-access（或要求临时降权）；③ Python 版本确认与安装授权。*